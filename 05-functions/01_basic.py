def square_of_num(number):
     return number ** 2

result = square_of_num(4)
print(square_of_num(5))
    #() these are parameters, actually placeholders but prefer to say parameters instead of arguments
    
print(result)